# URL of the Sparked NestJS backend. Used server-side only:
# Server Components, route handlers, Server Actions. Not exposed to the browser.
API_URL=

# Same URL, embedded in the client JS bundle so browser code can hit
# the backend directly (Client Components, useEffect fetch, etc.).
NEXT_PUBLIC_API_URL=
